{
  "name": "@public-function/unique-promise",
  "version": "0.1.0",
  "author": "Edward Bock <hi@edwardbock.de> (https://www.edwardbock.de/)",
  "description": "Group promises by id and execute only one at a time.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-typescript": "^7.21.4",
    "jest": "^29.5.0",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^5.0.4"
  }
}
